#personal aliases
alias brs="source ~/.bashrc"
alias brn="nano ~/.bashrc"

alias gitt="git clone"
alias c="cp -f"
alias r="rm -rf"
alias m="mv -f"
alias downloads="cd $HOME/storage/downloads && ls"
alias ins="pkg install"
alias pip="pip install"
alias pip2="pip2 install"
alias pipi="pip install -r requirements.txt && ls"
alias pipi2="pip2 install -r requirements.txt && ls"
alias py="python"
alias py2="python2"
alias b="bash"
alias r="rm -rf"
alias root="chmod +x"
alias rooot="chmod +x *"
alias clearrr="cd && clear && ls"
alias clearr="clear && ls"

alias downloads="cd $HOME/storage/downloads && ls"

#tools shortcut

alias cupp="cd && cd tools && cd cupp && python cupp.py -i"
alias bomb="cd && cd tools && cd XLR8_BOMBER && python xlr8.py"
alias pm="cd && cd tools && cd PhishMailer && py PhishMailer.py"
alias insta="cd && cd tools && cd Scylla && py scylla.py --instagram"
alias username="cd && cd tools && cd Scylla && py scylla.py --username"
alias ph="cd && cd tools && cd Scylla && py scylla.py -r"
alias phish="cd && cd tools && cd zphisher && bash zphisher.sh"


























ENABLE_CORRECTION="true"
